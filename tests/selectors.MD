# Selectors

## Syntax 
HTML tag #Value of ID attribute

example
input#name

## Syntax 
HTML tag [attribute = value of attribute]
example

input[placeholder='Enter Name']

# Notes

1. ll type of grouping like radio button, <br> list will be done within the class  

2. If I have no attibute and Text then use XPath

3. CSS dont have a locator to locate the text, so its better to use xpath

4. use ctrl + \ for freezing the however ove menu options to pause the script execution


